{
  "private": true,
  "name": "figma-open-graph-server",
  "version": "1.0.0",
  "description": "Provides the server component for figma-open-graph.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Jag Talon",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "node-html-parser": "^1.2.20"
  }
}
